version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "📦 amplify.yml FOUND (preBuild)"
            - echo "🔧 Using Node via runtime-versions (Node 20)"
        build:
          commands:
            - echo "📦 amplify.yml FOUND (build)"
            - npm ci
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    runtime-versions:
      nodejs: 20
